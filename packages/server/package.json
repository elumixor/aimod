{
  "name": "@aimod/server",
  "version": "1.0.0",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bunx --bun vite --force",
    "build": "vite build",
    "postbuild": "cp -r resources dist/resources",
    "prebuild": "rm -rf dist"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@elumixor/frontils": "^2.8.1",
    "chalk": "^5.3.0",
    "gpt-tokenizer": "^2.2.1",
    "openai": "^4.55.4",
    "ws": "^8.18.0"
  }
}
